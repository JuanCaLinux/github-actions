name: "test" 

on: [push]

jobs: 
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: ls
        run: | #comando para scripts multilinea
          echo "hola mundo" >> hola.txt
          cat hola.txt